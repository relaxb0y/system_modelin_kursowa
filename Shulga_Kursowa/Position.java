public class Position {
    public String Name;  // Сохраняет имя позиции (для вывода статистики в консоль)
    public int Number;  // Сохраняет номер позиции в сети
    public int Mark = 0;  // Сохраняет маркировку позиции
}
